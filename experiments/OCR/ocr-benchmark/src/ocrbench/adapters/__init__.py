"""adapters — per-engine OCR wrappers (PaddleOCR, Tesseract, ...).

NOT IMPLEMENTED YET (planned after engine screening, handbook Phase 3/8).
Each adapter implements the shared BaseOCREngine interface (to be defined in
ocrbench.core.base_engine) so evaluation code stays engine-agnostic.
"""
