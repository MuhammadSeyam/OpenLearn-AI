name: Test Workflow

on:
  pull_request:
    branches: [main]

jobs:
  hello:
    name: Say Hello
    runs-on: ubuntu-latest
    steps:
      - name: Print a message
        run: echo "Hello from GitHub Actions! CI is working."
